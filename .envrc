strict_env
if ! find_up .venv/pyvenv.cfg; then 
  python3 -m venv .venv
fi

source ./.venv/bin/activate

if ! find_up .venv/check.txt; then
  pip install .
  pip install '.[dev]'
  touch .venv/check.txt
fi
