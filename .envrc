strict_env

VENV_PATH=".venv"

if [ ! -d "${VENV_PATH}" ]; then 
  python3 -m venv ${VENV_PATH}
fi

source ${VENV_PATH}/bin/activate

if [ ! -f "./.direnv" ]; then
  pip install .
  pip install '.[dev]'
  touch ./.direnv
fi

pre-commit install
